[project]
name = "soundsig"
channels = ["conda-forge"]
platforms = ["linux-64"]

[pypi-dependencies]
soundsig = { path = ".", editable = true }

[tasks]
generate-test-data = "python scripts/generate_test_data.py"
